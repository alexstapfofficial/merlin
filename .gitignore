# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# macOS/iCloud duplicate copies (e.g. "package-lock 2.json")
* [0-9].*

# Raw geocoding/geonames data dumps (imported into the DB, not the repo)
/DE.txt
/DET.txt

# Local env files
.env
.env.*
!.env.example

/generated/prisma

# Capacitor native projects (regenerate via `npx cap add ios`)
/ios
/android
